query StopTimesList {
    stops {
        id
        name
        stoptimesWithoutPatterns {
            scheduledArrival
            realtimeArrival
            arrivalDelay
            scheduledDeparture
            realtimeDeparture
            departureDelay
            realtime
            realtimeState
            serviceDay
            headsign
            trip {
                gtfsId
                route {
                    gtfsId
                    shortName
                    longName
                    mode
                    type
                    desc
                    url
                }
                serviceId
                activeDates
            }
        }
    }
}
